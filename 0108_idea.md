# [PRD] 기후 행동 전략 수립을 위한 파워매핑(Power Mapping) 시각화 툴

## 1. 개요 (Overview)

- **목적:** 캠페인 액션(서한 발송, 기자회견, 소송 등) 실행 시, 이해관계자(Target)들의 역학 관계 변화를 시각적으로 시뮬레이션하고 추적하기 위함.
- **문제 의식:** 기존의 정적인 파워매핑으로는 액션 실행 전후의 타깃 변화를 파악하기 어렵고, 누구를 우선 타깃으로 삼아야 할지 모호함.
- **핵심 가치:** 액션에 따른 타깃의 위치 이동(지지도 및 영향력 변화)을 시각화하여 전략적 의사결정을 돕는다.

## 2. 핵심 로직: 4사분면 파워매핑 (The Quadrant)

각 타깃은 2차원 좌표평면 위에 위치하며, 두 가지 핵심 속성을 가짐.

### 2.1. 좌표 축 정의

- **X축: Support Level (지지도)**
  - 범위: `-100` (적극 반대) ~ `+100` (적극 찬성)
  - 기준: 암모니아 혼소 계획 철회에 대한 입장
    - `+100`: 계획 철회 적극 찬성
    - `-100`: 혼소 계획 고수 (철회 적극 반대)
- **Y축: Power Level (영향력)**
  - 범위: `-100` (영향력 없음) ~ `+100` (결정적 영향력)
  - 기준: 해당 계획의 실제 진행/취소에 미칠 수 있는 정치적, 경제적 결정권
    - `+100`: 최종 의사결정권자 혹은 막대한 여론 형성 가능
    - `-100`: 실제 프로세스에 관여 불가

### 2.2. 타깃 속성 (Properties)

- **소속 그룹:** 학회, 기업, 언론, 정부, NGO 등 (아이콘 또는 색상으로 구분)
- **민감도(Sensitivity):** 특정 액션(예: 언론 플레이)에 얼마나 민감하게 반응하여 좌표가 변하는지에 대한 가중치

## 3. 기술 스택 (Tech Stack)

퍼포먼스와 인터랙션 품질을 고려하여 아래 스택을 제안함.

- **Framework:** `Next.js` (App Router)
  - Vercel 배포 최적화 및 확장성 고려.
- **Styling:** `vanilla-extract`
  - Type-safe한 CSS 관리 및 빌드 타임 스타일 생성.
- **Animation:** `Framer Motion`
  - **핵심:** 타깃의 위치 이동(Transition)을 부드럽게 처리하고, 시뮬레이션 효과(Ghost UI) 구현에 필수.
- **State Management:** `Zustand` (제안)
  - 타깃 데이터 리스트와 시뮬레이션 상태(Before/After)를 전역에서 가볍게 관리하기 위함.
- **Math/Utility:** `D3.js` (`d3-scale`) (제안)
  - 데이터 값(-100 ~ 100)을 화면 픽셀 좌표로 정확하게 매핑하기 위한 유틸리티로 사용.
- **UI Component:** `Radix UI` (제안)
  - 접근성이 보장된 툴팁(타깃 상세 정보), 다이얼로그 등을 빠르게 구축.

## 4. 주요 기능 요구사항 (Functional Requirements)

### 4.1. 뷰어 & 필터링

- **좌표 렌더링:** 등록된 타깃들을 사분면 위에 점(또는 아이콘)으로 표시.
- **그룹 필터링:** '언론'만 보기, '기업'만 보기 등 토글 기능 제공.
- **상세 정보:** 타깃 아이콘 호버/클릭 시 이름, 소속, 현재 정확한 수치(Power/Support) 표시.

### 4.2. 액션 시뮬레이션 (Simulation Mode)

- **액션 선택:** 사용자가 '기자회견', '기후소송', '공개 질의서' 등의 액션 카드를 선택.
- **미리보기(Preview):**
  - 선택한 액션에 의해 영향을 받는 타깃들이 **어디로 이동할지 점선 화살표나 반투명 아이콘(Ghost UI)으로 미리 보여줌.**
  - 예: '기자회견' 선택 시 → 여론에 민감한 '학회' 그룹 타깃들의 지지도가 상승하는 방향으로 화살표 표시.
- **적용(Commit):** '실행' 버튼 클릭 시 실제 데이터가 업데이트되며 아이콘이 해당 위치로 부드럽게 이동.

### 4.3. 데이터 관리 (Data Management)

- 초기에는 클라이언트 사이드(LocalStorage) 저장 혹은 하드코딩된 JSON으로 시작하되, 추후 협업을 위해 Supabase/Firebase 연동을 고려.

## 5. 데이터 구조 예시 (TypeScript)

TypeScript

```
type TargetGroup = '학회' | '기업' | '언론' | '정부' | 'NGO';

// 개별 타깃 정보
interface Target {
  id: string;
  name: string;
  group: TargetGroup;
  position: {
    power: number;   // -100 ~ 100
    support: number; // -100 ~ 100
  };
  // (선택사항) 특정 액션에 대한 민감도 가중치
  sensitivity?: {
    public_pressure?: number; // 여론전 민감도
    legal_risk?: number;      // 법적 리스크 민감도
  };
}

// 전략 액션 정보
interface ActionStrategy {
  id: string;
  type: 'press_conference' | 'lawsuit' | 'letter';
  name: string;
  description: string;
  // 액션 실행 시 변화 로직 (함수 또는 고정값)
  effect: (target: Target) => { powerDelta: number, supportDelta: number };
}
```

## 6. 논의 및 결정 필요 사항 (Discussion Points)

1. **좌표 충돌 처리:**

   - `Power: 50, Support: 50`인 타깃이 여러 명일 경우 겹쳐서 보일 수 있음. 이를 겹침 허용 후 클릭 시 리스트를 보여줄지, 약간의 오프셋을 주어 분산시킬지 결정 필요.

2. **변화 로직의 자동화 수준:**

   - **Semi-Auto (권장):** 액션별로 기본 변화량(예: 기업 Support -10)을 설정해두되, 시뮬레이션 단계에서 사용자가 드래그하여 미세 조정 후 저장하는 방식.
   - **Manual:** 액션 선택 후 사용자가 100% 수동으로 위치를 옮기는 방식.

3. **백엔드 구축 여부:**

   - 팀원 간 실시간 공유가 필요한지, 개인용 툴인지에 따라 DB 도입 여부 결정.
